// const products = require('./data');
// let values = products.map((prod) => [
//   prod.name,
//   prod.image,
//   prod.shape,
//   prod.size,
//   prod.price,
//   prod.text,
// ]);
// let mysql = require('mysql');

// let conn = mysql.createConnection({
//   host: '127.0.0.1',
//   user: 'root',
//   password: 'password',
//   database: 'gemstore',
// });

// // let sql = `INSERT INTO products (name, image, shape, size, price, text) VALUES ?`;
// // conn.query(sql, [values], (err, res) => {
// //   if (err) throw err;
// //   console.log('Number of records inserted: ' + res.affectedRows);
// // });
